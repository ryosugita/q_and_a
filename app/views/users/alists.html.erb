<table class="table table-striped">
    <thead>
        <tr>
            <th>回答</th>
        </tr>
    </thead>
    <tbody>
        <% @answers.each do |answer| %>

        <tr>
            <td><%= link_to answer.content, question_path(answer[:question_id]) %></td>
        </tr>
        <% end %>
    </tbody>
</table>  